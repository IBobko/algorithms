| `reverse.py` | Учебный скрипт, который пошагово разворачивает число `123` в обратном порядке и выводит объяснение каждой арифметической операции. |
| `two.py` | Учебный пример решения задачи Two Sum с подробным русскоязычным выводом состояния словаря на каждой итерации; в конце файла есть фрагмент LaTeX-разметки, из-за которого файл не является валидным Python-модулем. |
